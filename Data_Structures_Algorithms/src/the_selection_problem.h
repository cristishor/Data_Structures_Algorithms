#ifndef H_SELECTION_PROBLEM
#define H_SELECTION_PROBLEM

typedef enum selection_problem_methods {
  NAIVE_APPROACH = 0,
  BLIND,
  METHOD_COUNT
};

void test_selection_problem();

#endif // !H_SELECTION_PROBLEM
